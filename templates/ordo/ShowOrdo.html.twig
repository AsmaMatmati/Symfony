{% extends "base.html.twig" %}
{% block title %}   Liste  {% endblock %}
{% block content %}
    <br>
    <div  class="m-4">
        <center><h2 style="color: #2586f5;">Liste des Ordonnances</h2></center>

    </div>
    <br>
    <a href="{{ path('AddOrdo') }}" class=pull-right >+Ajouter Ordonnance</a>
    <br> <br>

    <table class="table table-striped table-hover">
        <thead style="backgournd: #2586f5;">



        <tr>
            <th>Id</th>
            <th>Descrption</th>
            <th>Medicament</th>
            <th >Actions</th>
        </tr>
        </thead>
        <tbody> <!-- table body -->
        {% for i in ord %}
            <tr>
                <td> {{ i.id}}</td>
                <td>{{ i.description }}</td>
                <td>{{ i.Medicament.nom }}</td>
                <td>
                    <a href="{{ path('edit',{'id':i.id}) }}"> Update </a>
                    |
                    <a href="{{ path('Supprimer',{'id':i.id}) }}">Remove</a> </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br> <br> <br> <br>


{% endblock  %}
